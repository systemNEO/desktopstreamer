# Random hex string, generated by install.sh on real installs.
# Wird Teil des RTMP-Pfads beim Publishen (rtmp://server/live/STREAM_KEY).
# Hier nur zur Persistenz für spätere Anzeige — MediaMTX selbst nutzt
# diese Var nicht (Auth ist Path-as-Secret).
STREAM_KEY=replace-me-with-random-hex
DOMAIN=
